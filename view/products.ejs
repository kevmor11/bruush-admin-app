<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/_head %>
  </head>
  <body>
    <% include ../partials/_header %>
    <a href="/edit_product" class="btn btn-primary position-absolute add-product">Add Product</a>
    <section class="product-table mt-5 pb-5">
      <div class="container-fluid">
        <div class="row">
          <div class="col-12 mt-2">
            <h1 class="text-center md-2">Products</h1>
            <table id="products" class="table table-striped table-bordered">
              <thead>
                <tr>
                  <th>ID</th>
                  <th>Shopify ID</th>
                  <th>Name</th>
                  <th>Discount Code</th>
                  <th>Edit</th>
                </tr>
              </thead>
              <tbody>
                <% products.forEach((product) => { %>
                  <tr>
                    <th><%= product.id %></th>
                    <td><%= product.product_shopify_id %></td>
                    <td><%= product.name %></td>
                    <td><%= product.discount_code %></td>
                    <td><a href="/edit_product?id=<%= product.id %>" class="btn btn-outline-primary">Edit</a></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
            <div class="text-center mt-3">
              <% if (page > 0) { %>
                <a href="/products?page=<%= page - 1 %>" class="btn btn-secondary">Previous</a>
              <% } %>
              <% if (productCount === 10) { %>
                <a href="/products?page=<%= page + 1 %>" class="btn btn-primary">Next</a>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </section>
    <script>
      $(document).ready(() => {
        $('#products').DataTable({
          paging: false,
          info: false
        });
      });
    </script>
  </body>
</html>